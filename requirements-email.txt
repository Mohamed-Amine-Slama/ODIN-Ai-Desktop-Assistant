# Email and calendar skills (read_email, send_email, list_events,
# create_event, delete_event). Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-email.txt
#
# Jarvis runs fine without any of this — the skills just stay unregistered
# until an account is connected (see README section on email/calendar setup).

# Google: Gmail + Calendar APIs, OAuth via a Desktop-app client you register
# yourself at console.cloud.google.com.
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.100.0

# Microsoft: Mail + Calendar via Graph, OAuth via a public-client app you
# register yourself at portal.azure.com. No separate Graph SDK needed —
# Graph is called directly over HTTPS with `requests`, already a core dep.
msal>=1.28.0
