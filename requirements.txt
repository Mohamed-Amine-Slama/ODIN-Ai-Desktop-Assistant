# Core - everything Jarvis needs to run in text mode.
anthropic>=0.69.0
python-dotenv>=1.0.0
psutil>=5.9.0
requests>=2.31.0
plyer>=2.1.0

# Screen awareness (see_screen) and clipboard
mss>=9.0.1
pillow>=10.0.0
pyperclip>=1.8.2

# Windows-only: system volume control
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.2.0; sys_platform == "win32"

# Basic speech (upgraded in requirements-voice.txt)
SpeechRecognition>=3.10.0
pyttsx3>=2.90
PyAudio>=0.2.14; sys_platform == "win32"

# Tests
pytest>=8.0.0
