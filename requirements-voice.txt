# Hands-free voice pipeline. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-voice.txt
#
# Kept separate because this pulls ~1GB of ML wheels and model weights.
# Jarvis runs fine in text mode without any of it.

# Audio capture. sounddevice ships working Windows wheels, unlike PyAudio.
sounddevice>=0.4.6
numpy>=1.24.0

# Wake word: openWakeWord ships a pretrained "hey_jarvis" model.
# No API key, no Picovoice account, no training.
openwakeword>=0.6.0
onnxruntime>=1.16.0

# Speech-to-text: local Whisper. Offline, accurate, and your audio never
# leaves the machine (the old SpeechRecognition backend posted it to Google).
faster-whisper>=1.0.0

# Text-to-speech: Edge neural voices (free, no key) + playback.
edge-tts>=6.1.0
pygame>=2.5.0
